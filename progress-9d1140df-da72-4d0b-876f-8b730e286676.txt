# Progress Log
Run: 9d1140df-da72-4d0b-876f-8b730e286676
Task: Build personal investment decision dashboard
Started: 2026-03-02T22:35:00+01:00

## Codebase Patterns
- Project uses Next.js 14 with App Router, TypeScript, Tailwind, shadcn/ui
- Tests use `node:test` + `tsx --test __tests__/**/*.test.ts`
- Test files use `__dirname` (not `import.meta.dirname`) because tsx runs as CJS
- Path alias `@/*` maps to project root (tsconfig paths)
- Prisma 5.x with SQLite, db file at `.data/financial.db`
- Singleton PrismaClient in `lib/db.ts` (globalThis pattern for dev hot-reload)
- Scripts: `npm run migrate` (prisma migrate dev), `npm run db:push` (prisma db push), `npm run db:seed` (tsx prisma/seed.ts)
- `.gitignore` excludes `.data/*.db`, `node_modules/`, `.next/`, `.env*`
- Server actions go in `lib/actions/` with `'use server'` directive at file top
- ActionResult<T> pattern: `{ success: true, data: T } | { success: false, error: string }` for typed error handling
- Seed data uses deterministic IDs prefixed with `seed-` for easy identification
- Server action tests work without Next.js runtime — 'use server' is a no-op when imported directly
- Test cleanup: use `deleteMany` with prefix filter on IDs (e.g., `txn-test-*`) for isolation
- Pure calculation functions in `lib/calculations.ts` — no DB deps, easy to test separately
- Holding updates are always done through `createTransaction` (atomic via `prisma.$transaction`)
- `getHoldings` accepts optional `currentPrices` map for computed fields (currentValue, gainLoss)
- Symbol normalization: `.trim().toUpperCase()` in transaction creation
- Pure utility modules (like csv-parser.ts, finnhub.ts) go in `lib/` directly, not in `lib/actions/`
- CSV parser uses simple split-based parsing with quoted field support — no external deps
- Import actions reuse existing server actions (e.g., `createTransaction`) for consistency rather than raw Prisma calls
- API client types go in `lib/types/` — separate type files per external API
- FinnhubClient uses mock mode when API key is unset — deterministic data seeded from symbol chars
- Rate limiting: sliding-window approach with `RateLimiter` class (reusable for other APIs)
- Static helper methods on client class (mapQuote, mapNewsItem, mockQuote, etc.) for easy testing

---

## 2026-03-02 22:35 - US-002: Install Prisma and define core database schema
- Installed prisma@5.22.0 and @prisma/client@5.22.0
- Created `prisma/schema.prisma` with SQLite datasource pointing to `.data/financial.db`
- Defined all 7 models: Portfolio, Holding, Transaction, Signal, DecisionQueueItem, ExecutionLog, NewsItem
- All models have proper relations, indexes, and cascade deletes
- Created `lib/db.ts` with singleton PrismaClient export (globalThis pattern)
- Added package.json scripts: migrate, db:push, db:generate, db:studio
- Ran `npx prisma generate` and `npx prisma db push` successfully
- Updated `.gitignore` to exclude `.data/*.db` files
- Files changed: prisma/schema.prisma, lib/db.ts, package.json, package-lock.json, .gitignore, __tests__/prisma-schema.test.ts
- **Learnings:** Prisma 7 changed datasource config (requires prisma.config.ts), so pinned to v5.x for stability. tsx test runner uses CJS so `__dirname` works but `import.meta.dirname` doesn't.
---

## 2026-03-02 22:45 - US-003: Seed script and Portfolio CRUD server actions
- Created `prisma/seed.ts` with deterministic demo data: 2 portfolios (Tech Growth, Dividend Income), 5 holdings, 6 transactions
- Added `db:seed` script to package.json using tsx
- Created `lib/actions/portfolio.ts` with 5 server actions: getPortfolios, getPortfolio, createPortfolio, updatePortfolio, deletePortfolio
- All actions use `'use server'` file-level directive
- ActionResult<T> discriminated union for typed success/error responses
- Input validation: rejects empty/whitespace names, defaults baseCurrency to EUR
- getPortfolio includes holdings and transactions via Prisma include
- Created `__tests__/portfolio-actions.test.ts` with 13 tests covering full CRUD cycle + edge cases
- All 32 tests pass, typecheck passes
- Files changed: prisma/seed.ts, lib/actions/portfolio.ts, __tests__/portfolio-actions.test.ts, package.json
- **Learnings:** 'use server' directive at file top is a no-op outside Next.js runtime, so server actions can be tested as regular async functions without mocking.
---

## 2026-03-02 22:55 - US-004: Transaction and Holding server actions with position calculation
- Created `lib/calculations.ts` with 3 pure functions: calculateAvgCostBasis, calculatePositionValue, calculateGainLoss
- Created `lib/actions/transaction.ts` with getTransactions and createTransaction (atomic holding update via prisma.$transaction)
- Created `lib/actions/holding.ts` with getHoldings (optional currentPrices for computed fields)
- BUY: upserts holding, recalculates weighted average cost basis
- SELL: decreases quantity, validates sufficient shares, removes holding if fully sold
- Input validation: symbol required, quantity > 0, price >= 0, portfolio must exist
- Symbol normalized to uppercase on creation
- Created `__tests__/calculations.test.ts` with 9 tests for pure calculation functions
- Created `__tests__/transaction-actions.test.ts` with 14 tests covering BUY, SELL, getTransactions, getHoldings computed fields, and input validation
- All 55 tests pass, typecheck passes clean
- Files changed: lib/calculations.ts, lib/actions/transaction.ts, lib/actions/holding.ts, __tests__/calculations.test.ts, __tests__/transaction-actions.test.ts
- **Learnings:** prisma.$transaction with interactive callback is the clean way to do atomic multi-model updates. Test isolation via prefixed IDs + deleteMany cleanup works well.
---

## 2026-03-02 23:05 - US-005: CSV import parser for portfolio transactions
- Created `lib/csv-parser.ts` with `parseTransactionCSV(csvString)` function
  - Split-based CSV parsing with quoted field support (no external deps)
  - Case-insensitive header matching, flexible column order
  - Validates: required fields present, numeric values valid (quantity > 0, price >= 0, fees >= 0), date parseable
  - Returns `{ valid: ParsedTransaction[], errors: ParseError[] }`
  - Symbol normalization to uppercase, type case-insensitive (buy/Sell → BUY/SELL)
- Created `lib/actions/import.ts` with `importTransactions(portfolioId, csvString)` server action
  - Validates portfolio exists, parses CSV, creates transactions sequentially via `createTransaction`
  - Returns `ImportResult` with imported count, parse errors, and transaction-level errors
- Created `__tests__/csv-parser.test.ts` with 26 tests: valid parsing, case handling, column ordering, empty/null input, all validation error types, mixed valid/invalid rows
- Created `__tests__/import-actions.test.ts` with 6 tests: successful import, parse errors, empty CSV, missing/invalid portfolio, transaction-level errors (insufficient shares)
- All 87 tests pass, typecheck and build pass clean
- Files changed: lib/csv-parser.ts, lib/actions/import.ts, __tests__/csv-parser.test.ts, __tests__/import-actions.test.ts
- **Learnings:** Pure utility modules (csv-parser) go in `lib/` directly. Import actions should reuse existing server actions for consistency rather than raw Prisma calls.
---

## 2026-03-02 23:15 - US-006: Finnhub API client with quote and news endpoints
- Created `lib/types/finnhub.ts` with typed interfaces: FinnhubQuote, FinnhubQuoteRaw, FinnhubNewsItem, FinnhubNewsItemRaw, FinnhubConfig
- Created `lib/finnhub.ts` with FinnhubClient class:
  - Configurable via constructor (apiKey, baseUrl, maxRequestsPerMinute) or env var FINNHUB_API_KEY
  - `getQuote(symbol)` — returns typed FinnhubQuote
  - `getCompanyNews(symbol, from, to)` — returns FinnhubNewsItem[]
  - `getMarketNews(category)` — returns FinnhubNewsItem[]
  - Sliding-window rate limiter: max 60 calls/minute (configurable)
  - Mock mode auto-activates when no API key — deterministic data seeded from symbol characters
  - Symbol normalization to uppercase on all methods
  - Static mapping helpers (mapQuote, mapNewsItem) for easy testing
- Created `__tests__/finnhub.test.ts` with 18 tests:
  - Mock mode activation/deactivation
  - getQuote: type correctness, symbol normalization, determinism, price consistency, previousClose math
  - getCompanyNews: array return, symbol in related field, all fields typed, symbol normalization
  - getMarketNews: array return, category passthrough, default category
  - mapQuote: raw-to-typed mapping
  - mapNewsItem: field passthrough
- All 105 tests pass, typecheck and build pass clean
- Files changed: lib/types/finnhub.ts, lib/finnhub.ts, __tests__/finnhub.test.ts
- **Learnings:** API client types in `lib/types/` per external API. Static methods on client class make unit testing straightforward. Deterministic mock data seeded from input (symbol chars) is better than random — enables snapshot-style assertions.
---
